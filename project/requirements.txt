# CAP 6640 Project: Hallucination Detection and Measurement in LLMs
# Python 3.8+

# Core
transformers>=4.35.0
# Pin to 2.x: Hugging Face script datasets (e.g. FEVER, natural_questions) are not supported on datasets 4.x
datasets>=2.14.0,<3.0.0
torch>=2.0.0
accelerate>=0.24.0
bitsandbytes>=0.41.0

# Evaluation
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0

# Utilities
tqdm>=4.65.0
python-dotenv>=1.0.0

# Optional: for semantic similarity
# sentence-transformers>=2.2.0
